<h1>All Items</h1>
<% @posts.each do |post| %>
  <p><%= link_to item.title, item %></p>
  <div class="media">
    <div class="media-body">
      <h4 class="media-heading">
        <%= link_to item.title, item %>
      </h4>
    </div>
  </div>
<% end %>
