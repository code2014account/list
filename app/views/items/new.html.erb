<h1>New Item</h1>

<div class="row">
  <div class="col-md-4">
    <p>Guidelines for items on your checklist</p>
    <ul>
      <li>Make sure it is important.</li>
      <li>Don't forget to check you're email.</li>
      <li>Add one fun item!</li>
    </ul>
  </div>
  <div class="col-md-8">
    <%= form_for @item do |f| %>
      <div class="form-group">

        <%= f.label :title %>

        <%= f.text_field :title, class: 'form-control', placeholder: "Enter item title" %>
      </div>
      <div class="form-group">
        <%= f.label :body %>
        <%= f.text_area :body, rows: 8, class: 'form-control', placeholder: "Enter item body" %>
      </div>
      <div class="form-group">
        <%= f.submit "Save", class: 'btn btn-success' %>
      </div>
    <% end %>
  </div>
</div>
